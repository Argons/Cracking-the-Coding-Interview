# include <iostream>
using namespace std;

// Explain what the following code does: ((n & (n-1)) == 0).

int main() {
    cout << "To check if n is a non-negative power of 2." << endl;
    return 0;
}
