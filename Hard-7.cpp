#include <iostream>
#include <string>
#include <vector>
using namespace std;

// Write a program to find the longest word made of other words.    

string findLongestWord(vector<string> &text) {
    // First sort the strings by length, then split the longest string into 
    // two parts, and search these two sub-strings in the text.
    // Time: O(N*logN + N*len(str)),  Space: O(N)
}

int main() {
    return 0;
}
